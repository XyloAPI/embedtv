{
  "name": "embedstreaming",
  "version": "1.0.0",
  "description": "Minimal backend for HLS and DASH embed player pages",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}
